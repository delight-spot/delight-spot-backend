version: '3.8'
services:
  app:
    build: .
    volumes:
      - .:/project
    ports:
      - "8000:8000"
    image: app:django #app이라는 이미지를 만들었다. tag= django
    container_name: delight-spot
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - PYTHONUNBUFFERED=1